# OSINT MCP Server Configuration
# Copy this file to .env and customize as needed

# Rate limiting (requests per minute)
OSINT_RATE_LIMIT=10

# User agent for web requests
OSINT_USER_AGENT=OSINT-MCP-Server/0.1.0 (Educational/Research Purpose)

# Redis cache URL
REDIS_URL=redis://localhost:6379/0

# Optional API Keys (for enhanced functionality)
# Get your key from https://www.abuseipdb.com/
# ABUSEIPDB_API_KEY=your_key_here

# IPInfo API key (optional, for enhanced IP geolocation)
# IPINFO_API_KEY=your_key_here

# Shodan API key (optional, for port scanning and service detection)
# SHODAN_API_KEY=your_key_here

# Gradio connector allowlist (comma-separated base URLs)
# Only URLs in this list can be proxied to prevent open proxy behavior
# Example: OSINT_CONNECTOR_ALLOWLIST=https://example.gradio.app,https://another.gradio.app
OSINT_CONNECTOR_ALLOWLIST=
